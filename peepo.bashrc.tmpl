# Instantiated from peepo.bashrc.tmpl.
# This file is sourced for all commands and all shell blocks.
# Feel free to add your own content to peepo.bashrc.
alias grep="grep --color"

# The curl progress output is messed up in raw mode.
# Which causes problems when the input file changes after we start listening for keys in raw mode.
alias curl="curl -s"

alias git="git --no-pager"
